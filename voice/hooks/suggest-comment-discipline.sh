#!/usr/bin/env bash
echo "Load the comment-discipline skill (/voice:comment-discipline) before writing or editing code this session. Comments are only for non-obvious nuances — never restate the code, never narrate the current task or decision, never applied as a systematic habit."
